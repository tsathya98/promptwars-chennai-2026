name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  checks:
    name: Lint, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Verify repository guidance mirrors
        run: |
          diff -qr .claude/skills .agents/skills
          cmp -s CLAUDE.md AGENTS.md
      - name: Lint
        run: pnpm nx lint web
      - name: Test with coverage
        run: |
          pnpm --filter @promptwars/web exec vitest run --coverage.enabled --coverage.reporter=text-summary --coverage.reporter=text 2>&1 | tee /tmp/vitest.log
          { echo '### Test coverage'; echo '```'; grep -A 10 "Coverage report" /tmp/vitest.log | head -12; echo '```'; } >> "$GITHUB_STEP_SUMMARY" || true
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level=high
      - name: Build
        run: pnpm nx build web

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Scan for secrets
        uses: gitleaks/gitleaks-action@v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
